#!/usr/bin/python

from distutils.core import setup

setup(name='confluence',
      version='1.0',
      description='Python modules for Confluence wiki',
      author='Jonathan Reed',
      packages=['confluence']
      )
